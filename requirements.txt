# Quizzer - Requirements
# Python 3.10+ required

# No external dependencies required for core functionality
# All functionality uses Python standard library:
# - csv: CSV file parsing
# - json: JSON serialization
# - argparse: Command-line argument parsing
# - dataclasses: Data structure definitions
# - datetime: Timestamp generation
# - pathlib: Cross-platform path handling

# Optional dependencies for web interface
Flask>=3.0.0           # For web-based quiz interface (optional)
cryptography>=41.0.0   # For HTTPS support (self-signed certificates)

# Development dependencies
pytest>=7.0.0          # For running tests
pytest-cov>=4.0.0      # For test coverage reports
pytest-html>=3.2.0     # For HTML test reports
mypy>=1.0.0            # For type checking
black>=23.0.0          # For code formatting (uncompromising formatter)
isort>=5.12.0          # For import sorting
ruff>=0.1.0            # For fast linting (replaces flake8, pylint)
bandit>=1.7.0          # For security analysis
